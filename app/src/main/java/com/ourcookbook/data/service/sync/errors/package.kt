package com.ourcookbook.data.service.sync.errors

/**
 * Sync Error Service Package
 * 
 * Contains:
 * - SyncErrorService: Hilt module for sync error services
 * - SyncErrorNotificationService: Shows notifications for sync errors
 * - SyncErrorRecoveryService: Attempts to recover from sync errors
 * - SyncErrorReportingService: Reports sync errors to analytics
 * - SyncErrorWorkManager: Manages work for retrying sync errors
 */
