package com.ourcookbook.domain.repository

/**
 * Package index for repository interfaces
 * This file provides a central location for all repository interfaces in the domain layer
 */

// Recipe repository
export interface RecipeRepository

// Ingredient repository
export interface IngredientRepository

// RecipeImage repository
export interface RecipeImageRepository

// Device repository
export interface DeviceRepository

// DevicePreferences repository
export interface DevicePreferencesRepository

// Cookbook repository
export interface CookbookRepository

// SharingLink repository
export interface SharingLinkRepository

// SyncConflict repository
export interface SyncConflictRepository

// SyncLog repository
export interface SyncLogRepository

// PendingSync repository
export interface PendingSyncRepository

// SyncMetadata repository
export interface SyncMetadataRepository

// DriveFileInfo repository
export interface DriveFileInfoRepository

// Tombstone repository
export interface TombstoneRepository