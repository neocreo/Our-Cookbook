package com.ourcookbook.domain.usecase.sync.errors

/**
 * Sync Error Handling Package
 * 
 * Contains:
 * - SyncErrorHandler: Comprehensive error handling for sync operations
 * - SyncError: Sealed class with all error types
 * - SyncErrorWithContext: Error with additional context and metadata
 * - ErrorAction: Actions that can be taken for specific errors
 * - ErrorCategory: Categories for error classification
 * - ErrorSeverity: Severity levels for errors
 */
