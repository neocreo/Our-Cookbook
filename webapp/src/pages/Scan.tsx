// Scan — placeholder. Camera + OCR lands in Phase 3.

export function Scan() {
  return (
    <main className="page">
      <h1 className="page-title">Scan</h1>
      <p className="text-muted">Camera + OCR arrives in Phase 3.</p>
    </main>
  )
}
